## WIP
- favicon — being updated in a separate chat, do not touch
- postcards — Pikmin Bloom-style keepsake per visited POI. Types + context methods (`sendPostcard`, `openPostcard`, `seedMockPostcard`) and `PostcardsSection` component scaffolded; send flow now has a paper-plane send animation (`PoiDetailPanel.tsx`, `postcardPlaneOut` keyframe in `index.css`). Journal integration still not built.
- premium payment — client (`src/lib/billing.ts`, RevenueCat's `@revenuecat/purchases-capacitor` SDK) and server (`supabase/functions/revenuecat-webhook`, `premium_entitlements` table in `supabase/migrations/20260721000000_premium_entitlements.sql`) scaffolding is written; `PremiumModal.tsx` calls it when online+native, and `ProfileContext.tsx` reconciles `isPremium` against the server row once online. Not live yet - needs, in order: (1) a Google Play Console app for `com.lorewalk.app` with `lorewalk_premium_monthly` / `lorewalk_premium_yearly` subscription products, (2) a RevenueCat project linked to Play Console with a `premium` entitlement + Monthly/Annual packages on the default offering, its public API key set as `VITE_REVENUECAT_ANDROID_KEY`, (3) `supabase functions deploy revenuecat-webhook` with `REVENUECAT_WEBHOOK_SECRET` set (same value entered in RevenueCat's webhook config) and the migration applied, (4) at least an internal-testing Play Console release to test a real purchase on-device (Play Billing doesn't work in a plain browser tab). Coin packs in `ShopPage.tsx` are a separate, still-unwired purchase flow.

## Backlog
- premium: pick a medal supplier and get a real quote (see CLAUDE.md cost breakdown), then finalize Premium price point. Candidates worth quoting (low MOQ fits the monthly-changing-design model better than 100+ MOQ local SG suppliers): Medal Foundry (10-25pc runs), Made by Cooper (MOQ 50), EverLighten (no MOQ).
- premium: design + build the monthly event (progress tracking, completion claim, generate a claimable QR code for in-person pickup at the meetup instead of shipping-address capture).
- premium: set up the recurring monthly Meetup listing for the Singapore community event (5k walk + free drinks + QR-code medal pickup).
- player customization: swap the procedural avatar for a real CC0 rig. Manually download Quaternius Universal Base Characters + Modular Character Outfits (itch.io, name-your-own-price) or Kenney Modular Characters, export with `Skin`/`Hair`/`Eyes` materials and `Top_`/`Bottom_`/`Shoes_`/`Head_`-prefixed nodes matching `src/data/cosmetics.ts` ids, drop in as `public/models/player-avatar.glb` - see `public/models/README.md`. No code changes needed once the file exists.
- icons: food/shrine/squad/claim markers currently render `creaturePreview.ts`'s generic hashed-color placeholder cube (`getPlaceholderPreviewURL`, used via `EmojiSprite`/`MapView.tsx`'s `spriteEl()`) since they have no bespoke model - reads as a "missing icon" (a slightly rotated box). Source real CC0 low-poly models per marker type from Quaternius (quaternius.com) or Poly Pizza (poly.pizza, filter by license) and wire them into `creaturePreview.ts` the same way `getCreaturePreviewURL`/`getEggPreviewURL` already do for creatures/eggs.
